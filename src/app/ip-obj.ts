export interface IpObj {
  ip: string;
  city: string;
  region_code: string;
  postal: string;
  latitude: number;
  longitude: number;
  utc_offset: string;
  org: string;
  error: boolean;
  reserved: boolean;
}
